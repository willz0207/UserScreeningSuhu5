<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.userscreeningsuhu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_suhu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_suhu"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserScreeningSuhu"
        tools:ignore="AllowBackup">
        <activity android:name=".SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> </activity>
        <activity android:name=".history.RiwayatAdminActivity" />
        <activity android:name=".login.AdminLoginActivity"
            android:theme="@style/Theme.UserScreeningSuhu" />
        <activity android:name=".HasilScanActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".login.LoginActivity"
            android:theme="@style/Theme.UserScreeningSuhu"/>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.UserScreeningSuhu" />
    </application>
</manifest>